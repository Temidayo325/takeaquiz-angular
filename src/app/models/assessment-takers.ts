export interface AssessmentTakersResponse
{
     current_page: number,
     data: Array<any>,
     first_page_url: string|null,
     from: number|null,
     last_page: number|null,
     last_page_url: string|null,
     links: Array<any>,
     next_page_url: string|null,
     path: string,
     per_page: number,
     prev_page_url: string|null,
     to: number,
     total: number
}
